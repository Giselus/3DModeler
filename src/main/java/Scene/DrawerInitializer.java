package Scene;

import EntityTree.Entity;
import EntityTree.EntityModel;

public class DrawerInitializer {
//    public static void initialize(Entity entity, IRenderer renderer) {
//        if(entity instanceof EntityModel){
//            IModelDrawer drawer = renderer.getModelDrawer();
//
//        }
//            ((EntityModel) entity).setDrawer(new (((EntityModel) entity).getFaces(), renderingUpdater));
//
//        for(Entity child : entity.getChildren())
//            initialize(child, renderingUpdater);
//    }
}
